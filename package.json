{
  "name": "js-interview",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "PORT=3333 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "serve-api": "json-server --watch ./api/db.json --port 3334"
  },
  "devDependencies": {
    "json-server": "^0.12.1"
  }
}
